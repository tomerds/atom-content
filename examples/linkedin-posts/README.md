# LinkedIn Posts — Examples

Approved, high-quality LinkedIn posts that have actually gone out. These are the
ground truth for voice and structure, alongside the rules in
`playbooks/linkedin/`.

Drop approved posts here as Markdown, one file per post. Name them after the topic
(e.g. `ai-watershed-moment.md`). The `linkedin-post` skill reads these before
writing.
